

var compose = function(functions) {
    console.log(functions)
    // return function(x) {
        
    // }
};

compose([x => x + 1, x => 2 * x])
// const fn = compose([x => x + 1, x => 2 * x])
// console.log(fn(4))



/**
 * const fn = compose([x => x + 1, x => 2 * x])
 * fn(4) // 9
 */
